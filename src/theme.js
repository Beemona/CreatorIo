
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFD5AB',
    },
    secondary: {
      main: '#B7472A', 
    },
    calendar: {
      main: '#FF6A0D'
    }
  },
});

export default theme;
